# Audit every workflow's explicit least-privilege GITHUB_TOKEN boundary.
rules:
  dependabot-cooldown:
    # GitHub applies a three-day default to version updates while security
    # updates bypass cooldown. Preserve that upstream security/freshness balance.
    config:
      days: 3
  dangerous-triggers:
    ignore:
      # The workflow_run and pull_request_target jobs consume only the pinned
      # central Action, validate live GitHub state, and never check out or
      # execute pull-request content.
      - native-auto-merge.yml
